/**
 * Next.js app config for MetricForge (App Router). Currently uses defaults.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

/**
 * Exported Next.js configuration object.
 */
export default nextConfig;
