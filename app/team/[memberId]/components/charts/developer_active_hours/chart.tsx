/**
 * Placeholder active-hours line chart for the member profile.
 * Skeleton export — chart body not implemented beyond the component shell.
 */
import { ChartLineLabel } from "@/app/components/charts/[memberId]Charts/line_chart"

/**
 * Placeholder active-hours line chart for the member profile.
 * @returns Placeholder chart component for the member detail layout.
 * @example
 * <ActiveHoursLineChart />
 */
export default function ActiveHoursLineChart() {
    return (
        <div>
            <ChartLineLabel />
        </div>
    )
}
